:toc:
:toclevels: 3
:source-highlighter: highlight.js
ifdef::env-github[]
:tip-caption: :bulb:
:note-caption: :information_source:
:warning-caption: :warning:
endif::[]

= QB RuBQ wrapper component

toc::[]

== Intention

The main task of this component is to manage the communication between the Qanary pipeline and the RuBQ API and to prepare the data.

== Behavior

To do this, the component fetches the submitted question and sends the text attribute to the RuBQ API. The result from the API is processed, stored in the triple store and is then available in the Qanary pipeline.

As a Qanary component, it reads the question and the annotations produced by upstream components from the Qanary triplestore (the shared process memory), performs its task — build a formal (SPARQL) query from the question and its annotations — and writes the result back as standard Qanary (Web Annotation / `qa:`) annotations.

== Characteristics

* *Task type:* Query Building (QB).
* *Note:* This Qanary component fetches the SPARQL query for the enriched question from the RuBQ API.
* *Offline build:* builds and (where present) tests run offline.

== Implementation

* *Language:* Java (JDK 21).
* *Major frameworks:* Spring Boot 3.5 (`spring-boot-starter-parent`); the Qanary component framework `eu.wdaqua.qanary:qa.component` 4.0.0 (transitively Spring Boot Admin client + Apache Jena 5); springdoc-openapi (Swagger UI); Gson.
* *Main component class:* `src/main/java/eu/wdaqua/qanary/component/rubqwrapper/qb/RuBQQueryBuilder.java` (extends `eu.wdaqua.qanary.component.QanaryComponent`).
* *Tests:* 6 test class(es) under `src/test/java/` (`CacheTests.java`, `MockRuBQQueryBuilderTest.java`, `QanaryServiceControllerTest.java`, `RuBQQueryBuilderTest.java`, `RuBQTestConfiguration.java`, `SwaggerUiAvailabilityTest.java`).

== Build and run

Targets the current Qanary framework 4.0.0 (Spring Boot 3, Java 21). Install the
framework first (`mvn install` in the https://github.com/WDAqua/Qanary[Qanary]
repository), then build with JDK 21:

[source,bash]
----
JAVA_HOME=/path/to/jdk21 mvn clean package
docker build --build-arg JAR_FILE=target/qanary-component-qb-rubqwrapper.jar -t qanary/qanary-component-qb-rubqwrapper .
----

== Configuration

The component uses a cache to minimise the processing time.
For the configuration you can change the following parameter as you need:

----
qanary.webservicecalls.cache.specs=maximumSize=10000,expireAfterAccess=3600s
----

For the TeBaQA API you can change the following parameter as you need:

----
rubq.endpoint.url=
rubq.threshold=0.5
rubq.endpoint.language.default=en
rubq.endpoint.language.supported=en,ru
----

**rubq.endpoint.url**: The URL of the API endpoint,
make sure that the API accepts requests with the parameters
"question" for the question text.

**rubq.threshold**: The threshold for the answer.

**rubq.endpoint.language.default**: The default language of the API.

**rubq.endpoint.language.supported**: The list of supported languages of the API,
e.g. `rubq.endpoint.language.supported=en,ru`.

== Further references

- https://github.com/WDAqua/Qanary/wiki//How-to-start-a-standard-java-Qanary-component[How to start a standard java Qanary component]
