version: '3'
services:
  qanary-component-rd-python-predicateidentifier: # for building from source
    # or use image: qanary/qanary-component-rd-python-predicateidentifier:latest
    image: qanary/qanary-component-rd-python-predicateidentifier:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    container_name: $SERVICE_NAME_COMPONENT
    network_mode: host # usage of ports also possible
